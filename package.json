{
  "name": "cprt",
  "version": "0.1.0",
  "private": true,
  "description": "Webflow custom code for the CPRT project — Lumos build",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
